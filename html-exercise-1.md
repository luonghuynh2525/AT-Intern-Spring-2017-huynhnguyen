# HTML + CSS Exercise

### Knowledge round-up

- **HTML**
  - Why is it generally a good idea to put CSS `<link>`s inside `<head></head>`?
  - Describe the most fundamental usage of form elements (eg. `<input>`, `<button>`,...).

- **CSS**
  - What is the difference between classes and IDs in CSS?
  - Describe `float` and how they work.
  - What is the difference between `padding` and `margin`?
  - What does following snippet do and when will we use it?
    ```css
    * {
      box-sizing: border-box;
    }
    ```

  - Describe `z-index` and how stacking boxes is formed.

### Playground

- Tuning previous layout by using box model technique.